import React from 'react';
import { InfinityConfigEditorProps, InfinityQueryEditorProps } from './types';

export const InfinityConfigEditor = (props: InfinityConfigEditorProps) => {
  return <>Config Editor</>;
};

export const InfinityQueryEditor = (props: InfinityQueryEditorProps) => {
  return <>Query Editors</>;
};

export const InfinityVariableEditor = () => {
  return <>Variable Editor</>;
};
